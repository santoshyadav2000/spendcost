# ============================================================================
# database — PostgreSQL StatefulSet
# ----------------------------------------------------------------------------
# There are NO database settings here. Everything (mode, image, credentials,
# storage, resources, probes, security context) is configured in ONE place:
# global.database in the top-level values.yaml. These templates read only from
# there. The keys below only affect resource naming/namespace and are rarely
# changed.
# ============================================================================
namespaceOverride: ""
nameOverride: ""
fullnameOverride: ""
